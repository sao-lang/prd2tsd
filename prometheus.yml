# Prometheus 配置
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "prd2tsd-api"
    static_configs:
      - targets: ["api:8000"]
    metrics_path: "/api/v1/metrics"
